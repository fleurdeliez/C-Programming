#include <stdio.h>

int main() {
    FILE *fpointer;
    char filename[100];
    char ch;

    printf("Input the file name to be opened: ");
    scanf("%s", filename);

    fclose(fpointer);

    return 0;
}